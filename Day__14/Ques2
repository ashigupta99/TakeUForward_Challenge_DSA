// optimal

class Solution {
public:
    int removeDuplicates(vector<int>& arr) {
        int ind=1,n=arr.size();
        for(int i=1;i<n;i++){
            if(arr[i]!=arr[i-1]) arr[ind++]=arr[i];
        }
        return ind;
    }
};
