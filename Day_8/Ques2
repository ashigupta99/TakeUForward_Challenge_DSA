// Optimal - O(n), SC- O(n)

int subarraysXor(vector<int>& arr, int k) {
    unordered_map<int,int> mp;
    mp[0] = 1;
    int xr = 0;
    int cnt = 0;
    for(int x : arr){
        xr^=x;
        int need = xr^k;
        cnt+= mp[need];
        mp[xr]++;
    }
    return cnt;
}
